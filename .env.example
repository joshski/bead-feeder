# GitHub OAuth Configuration
# Register a GitHub OAuth app at: https://github.com/settings/applications/new
#
# Required scopes for the OAuth app:
#   - repo: Full control of private repositories (for reading/writing issues and dependencies)
#
# When registering the OAuth app:
#   - Application name: Choose a descriptive name (e.g., "Bead Feeder")
#   - Homepage URL: Your app's homepage (e.g., http://localhost:5173 for local dev)
#   - Authorization callback URL: http://localhost:5173/auth/callback (adjust for production)

# GitHub OAuth Client ID - public identifier for your OAuth app
GITHUB_CLIENT_ID=

# GitHub OAuth Client Secret - keep this secret! Never commit the actual value.
GITHUB_CLIENT_SECRET=

# Optional: GitHub OAuth scopes (defaults to "repo" if not specified)
# GITHUB_OAUTH_SCOPES=repo

# API Server Configuration
PORT=3001

# Frontend Configuration (for Vite)
VITE_API_URL=http://localhost:3001
VITE_GITHUB_CLIENT_ID=
